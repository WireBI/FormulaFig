{
  "version": 2,
  "buildCommand": "npm run build --workspace=client",
  "outputDirectory": "client/.next",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://<YOUR-RAILWAY-PROJECT-URL>/api/:path*"
    }
  ]
}
